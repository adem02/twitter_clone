// This Page is de default outlet of Home
// Renders a list of all tweets (that are not responses)
import React from 'react'
import Tweet from '../components/Tweet'

const TweetsPage = () => {
    return (
        <Tweet />
    )
}

export default TweetsPage
